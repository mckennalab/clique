% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_mix.R
\name{tree_mix}
\alias{tree_mix}
\title{Parsimony lineage tree with PHYLIP MIX}
\usage{
tree_mix(
  indel_mat,
  method = c("camin-sokal", "wagner"),
  outgroup = NULL,
  all = FALSE,
  binary = NULL,
  output_prefix = NULL,
  missing = -1L,
  timeout = Inf
)
}
\arguments{
\item{indel_mat}{Output \verb{$matrix} from \code{\link[=build_indel_matrix]{build_indel_matrix()}}.}

\item{method}{Camin-Sokal (irreversible) or Wagner parsimony.}

\item{outgroup}{Optional cell name from \code{rownames(indel_mat)}.}

\item{all}{Return all equally parsimonious trees instead of the first.}

\item{binary}{Optional path or name for the PHYLIP \code{mix} executable.}

\item{output_prefix}{Optional prefix under which the generated MIX input,
menu, report, tree, and screen output are retained.}

\item{missing}{Integer value used for missing data.}

\item{timeout}{Process timeout in seconds (\code{Inf} for none).}
}
\value{
An \code{ape::phylo}, or an \code{ape::multiPhylo} when \code{all = TRUE}.
}
\description{
Runs the PHYLIP \code{mix} program on a binary expansion of the clique character
matrix. Each positive edit state becomes a presence/absence character at its
site; state \code{0} is absent for every edit character, and missing calls become
\verb{?}. This avoids incorrectly treating arbitrary edit-state integers as an
ordered scale.
}
\details{
Camin-Sokal parsimony is the default because it models irreversible gains of
edit alleles. Wagner parsimony permits gains and losses. PHYLIP can emit
multiple equally parsimonious trees; by default the first is returned and
the number found is recorded in \code{clique_tied_trees}. Set \code{all = TRUE} to
return all trees as an \code{ape::multiPhylo}.

PHYLIP MIX is an optional external dependency. It uses fixed working-file
names, so this wrapper always runs it in an isolated temporary directory.
}
\examples{
\dontrun{
tree <- tree_mix(im$matrix, method = "camin-sokal")
}
}
