% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indel_matrix.R
\name{build_indel_matrix}
\alias{build_indel_matrix}
\title{Build a cell-by-site indel character matrix}
\usage{
build_indel_matrix(
  lineage_df,
  sites = NULL,
  cell_col = NULL,
  event_col = "ce",
  reference_col = "reference",
  missing_threshold = 0.5,
  missing_value = -1L
)
}
\arguments{
\item{lineage_df}{A data frame from \code{\link[=read_longread_lineage]{read_longread_lineage()}} /
\code{\link[=read_10x_lineage]{read_10x_lineage()}} with a \code{read_name} column and the event column.}

\item{sites}{Optional character vector naming the cut sites; defaults to
\code{site1..siteN} inferred from the event strings.}

\item{cell_col}{Name of the column identifying cells (e.g. \code{"e0"} for the
extracted cell barcode). If \code{NULL}, each read is treated as its own cell.}

\item{event_col}{Name of the called-edit column (default \code{"ce"}).}

\item{reference_col}{Name of the reference column, used only to warn when
cells span multiple references (whose site layouts may differ).}

\item{missing_threshold}{Cells with more than this fraction of missing sites
are dropped.}

\item{missing_value}{Integer used to encode missing data (default \code{-1}).}
}
\value{
A list with:
\itemize{
\item \code{matrix}: integer matrix, rows = cells, cols = sites.
\item \code{state_map}: named list mapping each site to its edit-string -> state-int
dictionary (\code{NONE} is always \code{0}).
}
}
\description{
Converts the per-read \code{ce} (called-edit) strings that clique writes into the
integer character matrix expected by lineage-tracing tree solvers. Each \code{ce}
value is a \verb{_}-joined list of per-target calls (see \code{\link[=read_longread_lineage]{read_longread_lineage()}});
reads are grouped into cells, the majority call is taken per site, and each
distinct edit string is assigned an integer state. \code{0} is the uncut/\code{NONE}
reference state and \code{-1} (by default) denotes missing data.
}
\details{
The \code{ce} fields are split on \verb{_} in YAML target order. Multiple events within
one target remain a single compound state (their \code{&}-joined event string is
not split). Ties in the per-cell majority vote are resolved by the first
value returned by \code{\link[base:table]{base::table()}}.
}
\examples{
\dontrun{
lineage <- read_longread_lineage("sample.consensus.bam", tags = c("ce", "e0"))
im <- build_indel_matrix(lineage, cell_col = "e0")
dim(im$matrix)            # cells x sites
head(im$state_map$site1)  # edit string -> integer state
}
}
