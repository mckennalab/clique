% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fasta_to_yaml.R
\name{fasta_targets_to_yaml}
\alias{fasta_targets_to_yaml}
\title{Build a clique read-structure YAML from a multi-reference FASTA + target set}
\usage{
fasta_targets_to_yaml(
  fasta_file,
  targets,
  output_file,
  target_types = "Cas9WT",
  search_reverse_complement = TRUE,
  keep_references_without_targets = TRUE,
  merge = "ConcatenateBothForward",
  known_strand = TRUE,
  reads = list(list(type = "Read1", orientation = "Forward")),
  uppercase = TRUE
)
}
\arguments{
\item{fasta_file}{Path to a FASTA file with one or more reference records.}

\item{targets}{Target sequences: a character vector, or a data frame with a
\code{target} (or \code{sequence}) column and optionally a \code{type} column.}

\item{output_file}{Path to write the YAML.}

\item{target_types}{clique target type(s) used when \code{targets} is a plain
character vector (or a data frame without a \code{type} column): a single value
recycled to all, or a vector the same length as \code{targets}. Must be one of
the clique target types (e.g. \code{"Cas9WT"}, \code{"Cas9ABE"}, \code{"Cas9ABEPalindrome"}).
\code{PrimeEdit} is not accepted by this helper because it also requires a
\code{prime_edits} specification; add prime-edit targets directly to the YAML.}

\item{search_reverse_complement}{If \code{TRUE}, a target not found on the forward
strand is searched as its reverse complement; when found that way, the
reverse complement (the actual forward-strand substring) is written.}

\item{keep_references_without_targets}{Keep references in which no target
matched (emitting empty \code{targets}). If \code{FALSE}, such references are dropped.}

\item{merge}{clique merge strategy (\code{"Align"}, \code{"Concatenate"},
\code{"ConcatenateBothForward"}) or \code{NULL} to omit.}

\item{known_strand}{Value of the layout's \code{known_strand} flag.}

\item{reads}{A list describing read positions; each element is a list with
\code{type} (\code{Read1}/\code{Read2}/\code{Index1}/\code{Index2}/\code{Spacer}) plus, for reads,
\code{orientation} (default \code{"Forward"}) or, for a spacer, \code{spacer_sequence}.}

\item{uppercase}{Upper-case reference and target sequences in the output
(recommended; clique matches targets case-sensitively).}
}
\value{
Invisibly, a named list (per kept reference) of the matched targets
with their type and strand (\code{"+"}/\code{"-"}). The YAML is written to
\code{output_file}.
}
\description{
Reads a (multi-record) FASTA and a set of CRISPR target/protospacer sequences,
locates each target within every reference, and writes a read-structure YAML
that \verb{clique align} / \verb{clique collapse} accept. Every target written for a
reference is guaranteed to be a forward-strand substring of that reference's
sequence (clique's loader requires this), so targets that only occur on the
reverse strand are written as their reverse complement.
}
\examples{
\dontrun{
fasta_targets_to_yaml(
  fasta_file   = "amplicons.fa",
  targets      = c("GACGGCTATACAAGGCATCGCGG", "CTCGTCAATACACCTTACGGAGG"),
  output_file  = "read_structure.yaml",
  target_types = "Cas9ABE"
)
}
}
\seealso{
\code{\link[=generate_clique_script]{generate_clique_script()}}
}
