% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_cassiopeia.R
\name{tree_cassiopeia}
\alias{tree_cassiopeia}
\title{Cassiopeia tree reconstruction (greedy / ILP / hybrid)}
\usage{
tree_cassiopeia(
  indel_mat,
  solver = c("greedy", "ilp", "hybrid", "upgma", "neighbor_joining"),
  priors = NULL,
  missing = -1L,
  ...
)
}
\arguments{
\item{indel_mat}{Output \verb{$matrix} from \code{\link[=build_indel_matrix]{build_indel_matrix()}}.}

\item{solver}{One of \code{"greedy"}, \code{"ilp"}, \code{"hybrid"}, \code{"upgma"},
\code{"neighbor_joining"}.}

\item{priors}{Optional named list of per-site indel priors.}

\item{missing}{Integer value used to encode missing data in \code{indel_mat}.}

\item{...}{Named keyword arguments passed to the chosen Cassiopeia solver's
constructor. For \code{solver = "hybrid"}, \code{top_solver} and \code{bottom_solver} may
be supplied as Python solver objects; the defaults are VanillaGreedySolver
and ILPSolver, with \code{cell_cutoff = 10}.}
}
\value{
An \code{ape::phylo} object with original cell names as tip labels.
}
\description{
Wraps the Python \href{https://github.com/YosefLab/Cassiopeia}{Cassiopeia} package
via \code{reticulate}. Cassiopeia is an optional dependency: this function errors
with installation instructions if it isn't available.
}
\examples{
\dontrun{
tree <- tree_cassiopeia(im$matrix, solver = "greedy")
tree <- tree_cassiopeia(im$matrix, solver = "hybrid", priors = my_priors)
}
}
