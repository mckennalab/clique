% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indel_matrix.R
\name{indel_distance}
\alias{indel_distance}
\title{Convert an indel character matrix to a normalized Hamming distance matrix}
\usage{
indel_distance(indel_mat, weights = NULL, missing = -1L)
}
\arguments{
\item{indel_mat}{Output \verb{$matrix} from \code{\link[=build_indel_matrix]{build_indel_matrix()}}.}

\item{weights}{Optional numeric vector of per-site weights (length = sites).}

\item{missing}{Integer value used to encode missing data in \code{indel_mat}.}
}
\value{
A \code{dist} object suitable for \code{\link[ape:nj]{ape::nj()}}.
}
\description{
Distance between two cells is the fraction of their shared non-missing sites
that carry different states (optionally site-weighted). Pairs with no shared
non-missing site are assigned distance 1.
}
\examples{
\dontrun{
d <- indel_distance(im$matrix)
tree <- ape::nj(d)
}
}
