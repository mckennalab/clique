% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clique_cmd.R
\name{clique_run}
\alias{clique_run}
\title{Low-level invocation of the clique CLI}
\usage{
clique_run(subcommand, args = list(), echo = TRUE, timeout = Inf)
}
\arguments{
\item{subcommand}{Name of the clique subcommand (e.g. \code{"align"}, \code{"collapse"}).}

\item{args}{Named list of \code{--flag = value} pairs (see details).}

\item{echo}{If \code{TRUE}, stream stdout/stderr to the R console live.}

\item{timeout}{Timeout in seconds (\code{Inf} for none).}
}
\value{
Invisibly, a list with \code{status}, \code{stdout}, \code{stderr}, and \code{command}
(the resolved argv).
}
\description{
Runs a single \code{clique} subcommand and captures stdout/stderr. Used internally
by \code{\link[=align]{align()}} and \code{\link[=collapse]{collapse()}}; exported for users who need to call
subcommands or flags this package doesn't yet wrap.
}
\details{
Argument conventions for \code{args}:
\itemize{
\item \code{TRUE} becomes a bare \code{--flag}.
\item \code{FALSE}, \code{NULL}, and \code{NA} are dropped (so callers can pass optional flags
without conditional logic).
\item Other scalar values become \verb{--flag value} with \code{as.character()} coercion.
\item Element names are translated to the CLI's kebab-case flags: underscores
become dashes, so \code{output_bam_file} is sent as \code{--output-bam-file}.
}
}
\examples{
\dontrun{
# Equivalent to: clique collapse --input_bam_file in.bam \
#   --output_bam_file out.bam --read_structure rs.yaml --correct_only
clique_run("collapse", list(
  input_bam_file = "in.bam",
  output_bam_file = "out.bam",
  read_structure = "rs.yaml",
  correct_only = TRUE
))

# Print the version banner
clique_run("--version", list())
}
}
