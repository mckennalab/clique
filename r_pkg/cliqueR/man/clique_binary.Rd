% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{clique_binary}
\alias{clique_binary}
\title{Locate the clique binary}
\usage{
clique_binary(refresh = FALSE)
}
\arguments{
\item{refresh}{If \code{TRUE}, ignore the cached path and re-resolve.}
}
\value{
Absolute path to the \code{clique} executable.
}
\description{
Resolution order:
\enumerate{
\item \code{CLIQUE_BIN} environment variable, if set and executable.
\item \code{clique} on the user's \code{PATH} (via \code{\link[=Sys.which]{Sys.which()}}).
\item \code{rust_cmd/target/release/clique} relative to the current working
directory, for developers building from this monorepo.
}
}
\details{
The resolved path is cached for the session. Call with \code{refresh = TRUE}
to re-resolve (e.g. after rebuilding the binary).
}
\examples{
\dontrun{
# Default: looks at CLIQUE_BIN, then PATH, then rust_cmd/target/release/
clique_binary()

# Force re-resolution after rebuilding the Rust binary
clique_binary(refresh = TRUE)
}
}
