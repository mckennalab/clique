% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_viz.R
\name{plot_lineage_tree}
\alias{plot_lineage_tree}
\title{Plot a lineage tree with per-tip annotation boxes}
\usage{
plot_lineage_tree(
  tree,
  annotations = NULL,
  columns = NULL,
  mapping_column = NULL,
  layout = c("circular", "fan", "rectangular"),
  categorical_palette = clique_categorical_palette(),
  continuous_palette = "viridis",
  ring_width = 0.12,
  ring_gap = 0.04,
  tip_labels = FALSE,
  title = NULL
)
}
\arguments{
\item{tree}{A tree: an \code{ape::phylo} / \code{treedata} object, a path to a Newick
file, or a Newick string.}

\item{annotations}{A data frame of per-tip annotations (one row per tip),
or \code{NULL} to draw the bare tree.}

\item{columns}{Which annotation columns to draw as rings, innermost first.
Defaults to every column except \code{mapping_column}.}

\item{mapping_column}{The \code{annotations} column holding the tip identifiers
(matched against the tree's tip labels). Auto-detected when \code{NULL}.}

\item{layout}{One of \code{"circular"} (default), \code{"fan"}, or \code{"rectangular"}.}

\item{categorical_palette}{Colours for categorical annotations, assigned to
levels in order. Defaults to \code{\link[=clique_categorical_palette]{clique_categorical_palette()}}.}

\item{continuous_palette}{Gradient for continuous annotations: a viridis
option name (e.g. \code{"viridis"}, \code{"magma"}) or a vector of >= 2 colours.}

\item{ring_width}{Radial width of each annotation ring (fraction of the tree).}

\item{ring_gap}{Gap before each ring (fraction of the tree).}

\item{tip_labels}{Draw tip labels?}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot}/\code{ggtree} object (print it, or save with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}).
}
\description{
Renders a (by default circular) lineage tree with a concentric ring of filled
boxes at the leaves for each annotation column. Categorical annotations are
coloured with a colourblind-safe discrete palette; continuous annotations use
a gradient. Each annotation gets its own fill scale and legend.
}
\examples{
\dontrun{
plot_lineage_tree(
  "tree.nwk",
  annotations    = cell_meta,       # data frame with a tip-id column
  mapping_column = "cell_barcode",
  columns        = c("cluster", "expression"),  # categorical + continuous
  layout         = "circular"
)
}
}
