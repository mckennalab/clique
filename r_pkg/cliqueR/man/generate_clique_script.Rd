% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_script.R
\name{generate_clique_script}
\alias{generate_clique_script}
\title{Generate a bash script for the clique align (+ collapse) pipeline}
\usage{
generate_clique_script(
  read_structure,
  reference,
  read1,
  output_script,
  read2 = NULL,
  index1 = NULL,
  index2 = NULL,
  output_dir = ".",
  sample_name = NULL,
  clique_bin = NULL,
  samtools = "samtools",
  threads = 4L,
  aligner = "WFA",
  min_read_length = 50L,
  max_reference_multiplier = 2L,
  collapse = FALSE,
  correct_only = FALSE,
  sort_and_index = TRUE,
  validate_reference = TRUE,
  execute = FALSE
)
}
\arguments{
\item{read_structure}{Path to the read-structure YAML.}

\item{reference}{Path to the reference FASTA (for the name cross-check).}

\item{read1}{Path to the read-1 FASTQ (required).}

\item{output_script}{Path to write the bash script.}

\item{read2, index1, index2}{Optional additional read/index FASTQs.}

\item{output_dir}{Directory the script writes BAMs into (it is created).}

\item{sample_name}{Base name for outputs; defaults to the \code{read1} basename.}

\item{clique_bin}{Path to the \code{clique} executable; defaults to
\code{\link[=clique_binary]{clique_binary()}} when resolvable, else \code{"clique"} on \code{PATH}. The script also
honours a \code{CLIQUE_BIN} environment variable at run time.}

\item{samtools}{Path to \code{samtools}.}

\item{threads}{Threads for align / collapse / sort.}

\item{aligner}{clique \code{--aligner} value (\code{"WFA"}, \code{"Degenerate"}, \code{"Inversion"}).}

\item{min_read_length, max_reference_multiplier}{clique align tuning flags.}

\item{collapse}{If \code{TRUE}, also emit a \verb{clique collapse} step (forces
\code{sort_and_index}).}

\item{correct_only}{Passed to collapse as \code{--correct-only} (correct tags only,
no consensus).}

\item{sort_and_index}{Sort + index the aligned BAM (required before collapse,
and for IGV).}

\item{validate_reference}{Cross-check YAML reference names against the FASTA.}

\item{execute}{If \code{TRUE}, run the generated script with \code{bash} via \link[processx:processx]{processx}.}
}
\value{
Invisibly, the path to the written (and, if \code{execute}, run) script.
}
\description{
Writes a self-contained bash script that runs \verb{clique align} on the given read
structure and reads -- aligning to the references embedded in the read
structure and calling edit events (the \code{ce} BAM tag) -- then sorts and indexes
the BAM, and optionally runs \verb{clique collapse} for per-molecule consensus.
}
\details{
The reference FASTA is not a direct \code{clique} input (references live inside the
read-structure YAML). It is used here to sanity-check that the YAML's
reference names match the FASTA, and is recorded in the script header.

With \code{sample_name = "sample"}, the script writes \code{sample.aligned.bam},
optionally \code{sample.sorted.bam} plus its index, and, when \code{collapse = TRUE},
\code{sample.consensus.bam} under \code{output_dir}. The generated script requires
\code{bash}, the \code{clique} binary, and \code{samtools} when sorting is enabled.
}
\examples{
\dontrun{
generate_clique_script(
  read_structure = "read_structure.yaml",
  reference      = "amplicons.fa",
  read1          = "sample_R1.fastq.gz",
  read2          = "sample_R2.fastq.gz",
  output_script  = "run_clique.sh",
  collapse       = TRUE
)
}
}
\seealso{
\code{\link[=fasta_targets_to_yaml]{fasta_targets_to_yaml()}}, \code{\link[=clique_run]{clique_run()}}
}
