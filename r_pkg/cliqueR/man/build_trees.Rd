% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_trees.R
\name{build_trees}
\alias{build_trees}
\title{Build lineage trees with several methods at once}
\usage{
build_trees(
  indel_mat,
  methods = c("nj", "parsimony"),
  output_dir = NULL,
  args = list(),
  missing = -1L,
  on_error = c("warn", "stop")
)
}
\arguments{
\item{indel_mat}{A cell x site indel matrix, e.g. from \code{\link[=build_indel_matrix]{build_indel_matrix()}}.}

\item{methods}{Character vector of methods to run; any of \code{"nj"},
\code{"parsimony"}, \code{"iqtree"}, \code{"mix"}, \code{"vine"}, \code{"cassiopeia"}. Defaults to the
two methods that need no external dependency.}

\item{output_dir}{Directory to write one \verb{tree_<method>.nwk} per successful
method (created if needed). \code{NULL} (default) writes nothing and returns the
trees only.}

\item{args}{Named list of extra arguments per method, e.g.
\code{list(nj = list(method = "bionj"), cassiopeia = list(solver = "hybrid"))}.}

\item{missing}{Missing-state value used for the neighbor-joining distance and
up-front matrix validation. Default \code{-1L}.}

\item{on_error}{\code{"warn"} (skip a failed method, default) or \code{"stop"} (abort on
the first failure).}
}
\value{
A named list (one entry per requested method) of \code{ape::phylo} objects
(or \code{ape::multiPhylo} for methods returning several trees); \code{NULL} for any
method that was skipped or failed.
}
\description{
Runs any combination of the \code{cliqueR} tree reconstructions on one indel
character matrix and returns the resulting trees, optionally writing a Newick
file (\verb{tree_<method>.nwk}) for each. It is a thin dispatcher over
\code{\link[=tree_nj]{tree_nj()}}, \code{\link[=tree_parsimony]{tree_parsimony()}}, \code{\link[=tree_iqtree]{tree_iqtree()}}, \code{\link[=tree_mix]{tree_mix()}}, \code{\link[=tree_vine]{tree_vine()}}
and \code{\link[=tree_cassiopeia]{tree_cassiopeia()}} — the neighbor-joining distance is built internally
from \code{\link[=indel_distance]{indel_distance()}}, every other method receives \code{indel_mat} directly.
}
\details{
Methods whose backend is unavailable (a missing external binary, or
Cassiopeia's Python environment) fail per-method: with \code{on_error = "warn"}
(the default) that method is skipped with a warning and returned as \code{NULL}
while the others still run, so a bare call with the two dependency-free
methods works everywhere.
}
\examples{
\dontrun{
im <- build_indel_matrix(lineage_df)
trees <- build_trees(im$matrix,
                     methods = c("nj", "parsimony", "cassiopeia"),
                     args = list(cassiopeia = list(solver = "greedy")),
                     output_dir = "trees/")
rf_distance(trees$nj, trees$parsimony)
}
}
\seealso{
\code{\link[=tree_nj]{tree_nj()}}, \code{\link[=tree_parsimony]{tree_parsimony()}}, \code{\link[=tree_iqtree]{tree_iqtree()}}, \code{\link[=tree_mix]{tree_mix()}},
\code{\link[=tree_vine]{tree_vine()}}, \code{\link[=tree_cassiopeia]{tree_cassiopeia()}}, \code{\link[=rf_distance]{rf_distance()}}
}
