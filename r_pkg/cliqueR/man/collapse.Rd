% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collapse.R
\name{collapse}
\alias{collapse}
\title{Collapse aligned reads into UMI consensus sequences}
\usage{
collapse(
  input_bam,
  read_structure,
  output_bam,
  temp_dir = NULL,
  max_deletion = 0L,
  find_inversions = FALSE,
  fast_reference_lookup = FALSE,
  correct_only = FALSE,
  threads = 1L
)
}
\arguments{
\item{input_bam}{Path to an aligned BAM (typically the output of \code{\link[=align]{align()}}).}

\item{read_structure}{Path to the read-structure YAML.}

\item{output_bam}{Path to write the collapsed/consensus BAM.}

\item{temp_dir}{Scratch directory for sharded sorting. \code{NULL} uses the
tool's default.}

\item{max_deletion}{Maximum deletion length allowed during alignment.}

\item{find_inversions}{If \code{TRUE}, also search for inverted matches.}

\item{fast_reference_lookup}{If \code{TRUE}, use the kmer-based reference index.}

\item{correct_only}{If \code{TRUE}, correct tags without building consensus.}

\item{threads}{Number of worker threads.}
}
\value{
This development release errors because the wrapper is not yet
implemented.
}
\description{
Wraps \verb{clique collapse}. Takes an aligned BAM and produces a per-UMI
consensus BAM via clustering and stretcher-based consensus building.
}
\details{
This typed wrapper is reserved for the planned high-level API and currently
errors without invoking the CLI. Use \code{\link[=generate_clique_script]{generate_clique_script()}} with
\code{collapse = TRUE} for a complete workflow, or \code{\link[=clique_run]{clique_run()}} to invoke
\code{collapse} directly.
}
\examples{
\dontrun{
collapse(
  input_bam = "sample.aligned.bam",
  read_structure = "barcodes.yaml",
  output_bam = "sample.consensus.bam",
  threads = 8
)

# Tag correction only — no consensus building
collapse(
  input_bam = "sample.aligned.bam",
  read_structure = "barcodes.yaml",
  output_bam = "sample.corrected.bam",
  correct_only = TRUE
)
}
}
