% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_longread.R
\name{read_longread_lineage}
\alias{read_longread_lineage}
\title{Read a long-read lineage BAM (PacBio / Nanopore)}
\usage{
read_longread_lineage(
  bam_path,
  read_structure = NULL,
  tags = NULL,
  region = NULL,
  samtools = "samtools"
)
}
\arguments{
\item{bam_path}{Path to a clique BAM.}

\item{read_structure}{Optional path to the read-structure YAML used to
produce the BAM. Currently reserved for mapping UMI symbols to tag names;
tag discovery presently reads the BAM directly.}

\item{tags}{Character vector of BAM tag names to extract. If \code{NULL}, all aux
tags found on the first record are used (plus \code{ce} is always included).}

\item{region}{Optional region string (e.g. \code{"left"}) passed to \verb{samtools view} to restrict to one reference; requires an index for that BAM.}

\item{samtools}{Path to the \code{samtools} executable.}
}
\value{
A data frame keyed by \code{read_name} with a \code{reference} column and one
column per extracted tag (missing tags are \code{NA}).
}
\description{
Parses a BAM produced by the CLI's \verb{clique align} or \verb{clique collapse}
command into a per-read data frame, pulling the aux tags clique writes: the
called-edit string (\code{ce}), the read count (\code{rc}), and any extracted-UMI tags
(\verb{e<symbol>}, e.g. \code{e0} for the cell barcode). Reads are streamed through
\verb{samtools view}, so \code{samtools} must be on the \code{PATH} (or passed via
\verb{samtools =}); this avoids a Bioconductor/Rsamtools dependency.
}
\details{
Tag values are returned as character columns regardless of their SAM type.
Convert depths such as \code{rc} and \code{dc} explicitly before numeric summaries.
When \code{tags = NULL}, tags are discovered from the first BAM record; pass an
explicit vector when later records may contain additional annotations.
}
\examples{
\dontrun{
reads <- read_longread_lineage("sample.consensus.bam", tags = c("ce", "e0"))
}
}
\seealso{
\code{vignette("bam-output", package = "cliqueR")},
\code{\link[=build_indel_matrix]{build_indel_matrix()}}
}
