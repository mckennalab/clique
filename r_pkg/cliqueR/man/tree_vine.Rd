% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_vine.R
\name{tree_vine}
\alias{tree_vine}
\title{Bayesian lineage-tree reconstruction with VINE}
\usage{
tree_vine(
  indel_mat,
  model_type = c("sitewise", "global"),
  mutation_prior = c("uniform", "empirical"),
  dimensionality = NULL,
  threads = 1L,
  nsamples = 100L,
  nj_only = FALSE,
  return_posterior = FALSE,
  binary = NULL,
  output_prefix = NULL,
  extra_args = character(),
  missing = -1L,
  timeout = Inf
)
}
\arguments{
\item{indel_mat}{Output \verb{$matrix} from \code{\link[=build_indel_matrix]{build_indel_matrix()}}.}

\item{model_type}{CRISPR rate-matrix parameterization: one matrix per site or
one global matrix.}

\item{mutation_prior}{Uniform or empirical mutation-state frequencies.}

\item{dimensionality}{Embedding dimension. \code{NULL} uses VINE's logarithmic
default, capped at one less than the number of cells so small data sets
remain valid.}

\item{threads}{Positive thread count.}

\item{nsamples}{Number of posterior trees emitted after convergence.}

\item{nj_only}{If \code{TRUE}, return VINE's initial NJ/UPGMA tree without
variational inference.}

\item{return_posterior}{Attach parsed posterior samples to the returned mean
tree. Ignored for \code{nj_only = TRUE}.}

\item{binary}{Optional VINE executable name or path.}

\item{output_prefix}{Optional prefix for retained matrix, log, mean-tree, and
posterior-sample files. Temporary files are removed when this is \code{NULL}.}

\item{extra_args}{Additional VINE arguments not managed by the wrapper.}

\item{missing}{Integer missing-data value. VINE requires \code{-1}.}

\item{timeout}{Process timeout in seconds (\code{Inf} for none).}
}
\value{
An \code{ape::phylo} with original cell names as tip labels.
}
\description{
Writes clique lineage characters in VINE's native CRISPR matrix format,
runs variational inference, and returns the posterior mean tree. VINE models
state \code{0} as unedited, positive integers as edited states, and \code{-1} as
missing. Its CRISPR mode is ultrametric.
}
\details{
Set \code{nj_only = TRUE} for VINE's fast initialization tree without variational
inference. Otherwise, VINE writes posterior tree samples to standard output
and its \code{--mean} tree is returned. With \code{return_posterior = TRUE}, parsed
samples are attached as the \code{clique_posterior} attribute.

VINE is an optional external dependency available from Bioconda or Homebrew.
}
\examples{
\dontrun{
tree <- tree_vine(im$matrix, threads = 8, nsamples = 500)
fast_tree <- tree_vine(im$matrix, nj_only = TRUE)
}
}
