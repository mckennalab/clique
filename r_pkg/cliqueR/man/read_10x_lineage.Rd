% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_10x.R
\name{read_10x_lineage}
\alias{read_10x_lineage}
\title{Read a 10x CellRanger lineage-tracing output}
\usage{
read_10x_lineage(cellranger_dir, lineage_bam, min_reads_per_cell = 1L)
}
\arguments{
\item{cellranger_dir}{Path to a CellRanger \verb{outs/} directory (must contain
\verb{filtered_feature_bc_matrix/} for cell barcodes).}

\item{lineage_bam}{Path to the per-cell lineage BAM, typically the output
of \code{\link[=collapse]{collapse()}}.}

\item{min_reads_per_cell}{Drop cells with fewer than this many lineage reads.}
}
\value{
This development release errors because 10x import is not yet
implemented.
}
\description{
Loads a CellRanger output directory and pairs cell barcodes with their
lineage-cassette reads. Intended as input to \code{\link[=build_indel_matrix]{build_indel_matrix()}}.
}
\details{
This function is a reserved API in the current development release and
errors without reading either input. For clique BAM files that already carry
extracted cell-barcode tags, use \code{\link[=read_longread_lineage]{read_longread_lineage()}} and pass the
relevant tag (for example, \code{tags = c("e0", "ce")}).
}
\examples{
\dontrun{
lineage <- read_10x_lineage(
  cellranger_dir = "sample/outs",
  lineage_bam    = "sample.consensus.bam",
  min_reads_per_cell = 3
)
}
}
